# Domain Agent Notes

## Scope

`packages/domain` là lõi nghiệp vụ. Không import Fastify, React, database client, Docker SDK hoặc framework hạ tầng.

## Local Commands

```bash
pnpm --filter @benh-vien-so/domain run check
pnpm --filter @benh-vien-so/domain run test
pnpm --filter @benh-vien-so/domain run build
```

## Rules

- Với FHIR ClinicalDocument DocumentReference mapper, `map-clinical-document-to-fhir.ts` giữ public resource mapper và clinical references; profile, lifecycle status, document type labels và attachment content mapping nằm trong `map-clinical-document-codings.ts`. Chạy `pnpm run harness:domain-composition` khi đổi boundary clinical document FHIR.
- Với FHIR ClinicalDocument Provenance mapper, `map-clinical-document-to-fhir-provenance.ts` giữ public Provenance resource mapper, target DocumentReference và timestamp orchestration; profile, signed-document guard, policy URI normalization, activity, agent và source entity mapping nằm trong `map-clinical-document-provenance-codings.ts`. Chạy `pnpm run harness:domain-composition` khi đổi boundary provenance FHIR.

- Domain model phải bảo vệ invariant bằng method hoặc factory.
- Value object/aggregate dùng thông báo lỗi tiếng Việt có dấu khi lỗi có thể hiển thị cho người dùng.
- Mapping FHIR đặt trong namespace `fhir`, không trộn vào aggregate.
- Với `RecordTransfer`, aggregate `record-transfer.ts` giữ hành vi vòng đời và cập nhật snapshot; create/rehydrate snapshot normalization nằm trong `record-transfer.factory.ts`; status transition guard nằm trong `record-transfer.lifecycle.ts`; invariant thời gian/trạng thái snapshot nằm trong `record-transfer.snapshot-validation.ts`; text guard, date parser và code-set normalization nằm trong `record-transfer.validation.ts`; type, snapshot, command input và status set nằm trong `record-transfer.types.ts`. Chạy `pnpm run harness:domain-composition` khi đổi boundary domain này.
- Với `ProviderDirectory`, aggregate `provider-directory.ts` giữ assemble/rehydrate behavior; resource normalization nằm trong `provider-directory.validation.ts`; coding/telecom/date/code-set primitive guard nằm trong `provider-directory.primitives.ts`; cross-resource reference/uniqueness guard nằm trong `provider-directory.references.ts`; defensive snapshot cloning nằm trong `provider-directory.snapshots.ts`; type, snapshot, coding, telecom và endpoint code set nằm trong `provider-directory.types.ts`.
- Với `AuditEvent`, aggregate `audit-event.ts` giữ record/rehydrate behavior và re-export API integrity cũ; seal/integrity report behavior nằm trong `audit-event.integrity.ts`; action/resource literal catalog nằm trong `audit-event.catalog.ts`; snapshot và integrity report type nằm trong `audit-event.types.ts`.
- Với `AccessControl`, file `access-control.ts` giữ quyết định RBAC/ABAC công khai; chính sách PractitionerRole còn hiệu lực nằm trong `access-control.organization-scope.ts`; duyệt cây tổ chức active nằm trong `access-control.organization-tree.ts`; role, purpose và permission type nằm trong `access-control.policy.ts`; nhóm quyền lâm sàng nằm trong `access-control.clinical-permissions.ts`; nhóm quyền hành chính/chuyển hồ sơ nằm trong `access-control.permission-groups.ts`; role-permission catalog nằm trong `access-control.permissions.ts`.
- Với `Patient`, aggregate `patient.ts` giữ update/merge behavior; register/rehydrate props normalization nằm trong `patient.factory.ts`; gender, identifier, props, snapshot, registration input và code set nằm trong `patient.types.ts`.
- Với `WorkflowTask`, aggregate `workflow-task.ts` giữ lifecycle behavior; create/rehydrate snapshot normalization nằm trong `workflow-task.factory.ts`; code/reference structure và timeline guard nằm trong `workflow-task.validation.ts`; status/intent/priority/reference resource code-set guard nằm trong `workflow-task.code-set-guards.ts`; status, intent, priority, reference, snapshot và input type nằm trong `workflow-task.types.ts`.
- Với `Procedure`, aggregate `procedure.ts` giữ record/rehydrate behavior; coding, performer structure, report-reference, self-reference và lifecycle guard nằm trong `procedure.validation.ts`; status/category/performer actor/report reference code-set guard nằm trong `procedure.code-set-guards.ts`; status, category, performer, report reference, snapshot và input type nằm trong `procedure.types.ts`.
- Với `RecordTransferDeliveryAttempt`, aggregate `record-transfer-delivery-attempt.ts` giữ terminal update behavior; queue/rehydrate snapshot normalization nằm trong `record-transfer-delivery-attempt.factory.ts`; delivery normalization và terminal-state guard nằm trong `record-transfer-delivery-attempt.validation.ts`; status, bundle, snapshot và command input type nằm trong `record-transfer-delivery-attempt.types.ts`.
- Với miền thuốc, `medication-request.ts`, `medication-dispense.ts` và `medication-administration.ts` giữ prescribe/record/rehydrate behavior; medication code, dosage/quantity, handover/effective-period, performer structure và timeline/lifecycle guard nằm trong các file `*.validation.ts`; riêng `MedicationAdministration` đặt status/category/performer actor code-set guard trong `medication-administration.code-set-guards.ts`; status, category/intent/priority, dosage, performer, snapshot và command input type nằm trong các file `*.types.ts` tương ứng.
- Với `ServiceRequest`, aggregate `service-request.ts` giữ ordering/scheduling behavior; code/status/priority/timeline guard nằm trong `service-request.validation.ts`; status, intent, category, priority, snapshot và command input type nằm trong `service-request.types.ts`.
- Với `ImagingStudy`, aggregate `imaging-study.ts` giữ record/rehydrate behavior; DICOM UID, series/count và timeline guard nằm trong `imaging-study.validation.ts`; status, coding, series, snapshot và command input type nằm trong `imaging-study.types.ts`.
- Với `ClinicalDocument`, aggregate `clinical-document.ts` giữ signing behavior; create/rehydrate props normalization nằm trong `clinical-document.factory.ts`; attachment, status và timeline guard nằm trong `clinical-document.validation.ts`; document type, status, props, snapshot và command input type nằm trong `clinical-document.types.ts`.
- Với `DiagnosticReport`, aggregate `diagnostic-report.ts` giữ issue/rehydrate behavior; code, content, status/category và timeline guard nằm trong `diagnostic-report.validation.ts`; status, category, code, snapshot và command input type nằm trong `diagnostic-report.types.ts`.
- Với `Observation`, aggregate `observation.ts` giữ recording behavior; quantity/value/status/timeline guard nằm trong `observation.validation.ts`; status, category, coding, quantity, snapshot và command input type nằm trong `observation.types.ts`.
- Với `Condition`, aggregate `condition.ts` giữ recording và clinical status behavior; code/status/severity/timeline guard nằm trong `condition.validation.ts`; status, category, severity, code, snapshot và command input type nằm trong `condition.types.ts`.
- Với `AllergyIntolerance`, aggregate `allergy-intolerance.ts` giữ recording và reaction behavior; code/reaction/status/timeline guard nằm trong `allergy-intolerance.validation.ts`; status, category, reaction, snapshot và command input type nằm trong `allergy-intolerance.types.ts`.
- Với `Encounter`, aggregate `encounter.ts` giữ creation, lifecycle và finish behavior; class/status/timeline guard nằm trong `encounter.validation.ts`; class, status, snapshot và command input type nằm trong `encounter.types.ts`.
- Với `Consent`, aggregate `consent.ts` giữ revoke và authorization behavior; grant/persisted snapshot normalization nằm trong `consent.factory.ts`; period/revocation/status/category guard nằm trong `consent.validation.ts`; status, category, snapshot và command input type nằm trong `consent.types.ts`.
- Với `Patient`, aggregate `patient.ts` giữ demographic update và merge behavior; register/rehydrate props normalization nằm trong `patient.factory.ts`; identifier, FHIR birth date, merge state và timeline guard nằm trong `patient.validation.ts`; gender/identifier/status/props/snapshot/input type nằm trong `patient.types.ts`.
- Với `RecordTransfer`, aggregate `record-transfer.ts` giữ lifecycle behavior và snapshot mutation; snapshot creation/rehydration nằm trong `record-transfer.factory.ts`; status transition guard nằm trong `record-transfer.lifecycle.ts`; snapshot invariant guard nằm trong `record-transfer.snapshot-validation.ts`; primitive/code-set normalization nằm trong `record-transfer.validation.ts`; status, snapshot và command input type nằm trong `record-transfer.types.ts`.
- Với `ProviderDirectory`, aggregate `provider-directory.ts` giữ assembly behavior; resource normalization nằm trong `provider-directory.validation.ts`; primitive guard nằm trong `provider-directory.primitives.ts`; reference guard nằm trong `provider-directory.references.ts`; snapshot cloning nằm trong `provider-directory.snapshots.ts`; snapshot/code set/input type nằm trong `provider-directory.types.ts`.
- Với `AuditEvent`, aggregate `audit-event.ts` giữ record/rehydrate behavior; seal và integrity report behavior nằm trong `audit-event.integrity.ts`; canonical hashing, normalization và seal metadata guard nằm trong `audit-event.validation.ts`; action/resource literal catalog nằm trong `audit-event.catalog.ts`; snapshot/integrity report type nằm trong `audit-event.types.ts`.
- Với FHIR type, `fhir-types.ts`, `fhir-provider.types.ts`, `fhir-clinical.types.ts`, `fhir-clinical-core.types.ts`, `fhir-document.types.ts`, `fhir-medication.types.ts`, `fhir-careflow.types.ts` và `fhir-diagnostics.types.ts` chỉ là barrel tương thích; resource declaration nằm trong các module `fhir-*.types.ts` theo nhóm shared/provider organization-practitioner-practitioner-role-endpoint/document reference-provenance-composition/privacy/audit/clinical encounter-condition-observation-allergy/medication request-dispense-administration/careflow service request-task-procedure/diagnostics report-imaging/patient/operation/capability/bundle.
- Với FHIR CapabilityStatement, `build-capability-statement.ts` chỉ dựng instance metadata; catalog resource, documentation và default interaction nằm trong `capability-statement-resources.ts`. Chạy `pnpm run harness:domain-composition` khi đổi danh sách resource công bố.
- Với FHIR PatientRecord Bundle, `map-patient-record-to-fhir-bundle.ts` chỉ dựng collection Bundle envelope; input contract và resource union nằm trong `patient-record-bundle.types.ts`; resource collection và fullUrl entry mapping nằm trong `patient-record-bundle-resources.ts`; `map-patient-record-to-fhir-document-bundle.ts` tái dùng collection Bundle rồi thêm Composition đầu tiên; profile/identifier, metadata Composition và Composition first-entry mapping nằm trong `patient-record-document-composition.ts`; direct author/custodian reference resolution nằm trong `patient-record-document-references.ts`; section catalog, resource filtering và generated XHTML narrative nằm trong `patient-record-document-sections.ts`. Chạy `pnpm run harness:domain-composition` khi đổi boundary bundle này.
- Với FHIR Consent mapper, `map-consent-to-fhir.ts` giữ public resource mapper, references và revocation extension; profile, identifier, status, scope và category nằm trong `map-consent-codings.ts`; provision actor/action/purpose/class policy nằm trong `map-consent-provision-codings.ts`. Chạy `pnpm run harness:domain-composition` khi đổi boundary consent FHIR.
- Với FHIR Condition mapper, `map-condition-to-fhir.ts` giữ public resource mapper và clinical references; profile, status/category/severity labels và CodeableConcept conversion nằm trong `map-condition-codings.ts`. Chạy `pnpm run harness:domain-composition` khi đổi boundary condition FHIR.
- Với FHIR AllergyIntolerance mapper, `map-allergy-intolerance-to-fhir.ts` giữ public resource mapper và clinical references; profile, clinical/verification status labels, substance CodeableConcept và reaction manifestation mapping nằm trong `map-allergy-intolerance-codings.ts`. Chạy `pnpm run harness:domain-composition` khi đổi boundary allergy intolerance FHIR.
- Với FHIR Observation mapper, `map-observation-to-fhir.ts` giữ public resource mapper, value và clinical references; profile, observation-category và CodeableConcept conversion nằm trong `map-observation-codings.ts`. Chạy `pnpm run harness:domain-composition` khi đổi boundary observation FHIR.
- Với FHIR DiagnosticReport mapper, `map-diagnostic-report-to-fhir.ts` giữ public resource mapper, workflow references, result references và presented form; profile, diagnostic service category và CodeableConcept conversion nằm trong `map-diagnostic-report-codings.ts`. Chạy `pnpm run harness:domain-composition` khi đổi boundary diagnostic report FHIR.
- Với FHIR ImagingStudy mapper, `map-imaging-study-to-fhir.ts` giữ public resource mapper và clinical/PACS references; profile, DICOM identifiers, modality de-duplication và series projection nằm trong `map-imaging-study-codings.ts`. Chạy `pnpm run harness:domain-composition` khi đổi boundary imaging study FHIR.
- Với FHIR MedicationRequest mapper, `map-medication-request-to-fhir.ts` giữ public resource mapper và clinical references; profile, category label, CodeableConcept conversion, dosageInstruction và dispenseRequest mapping nằm trong `map-medication-request-codings.ts`. Chạy `pnpm run harness:domain-composition` khi đổi boundary request FHIR.
- Với FHIR MedicationDispense mapper, `map-medication-dispense-to-fhir.ts` giữ public resource mapper và clinical references; profile, identifier, category label, CodeableConcept conversion và dosageInstruction mapping nằm trong `map-medication-dispense-codings.ts`. Chạy `pnpm run harness:domain-composition` khi đổi boundary dispense FHIR.
- Với FHIR MedicationAdministration mapper, `map-medication-administration-to-fhir.ts` giữ public resource mapper và clinical references; profile, identifier, category label, CodeableConcept conversion, performer và dosage mapping nằm trong `map-medication-administration-codings.ts`. Chạy `pnpm run harness:domain-composition` khi đổi boundary administration FHIR.
- Với FHIR ServiceRequest mapper, `map-service-request-to-fhir.ts` giữ public resource mapper và clinical references; profile, SNOMED category catalog và CodeableConcept conversion nằm trong `map-service-request-codings.ts`. Chạy `pnpm run harness:domain-composition` khi đổi boundary service request FHIR.
- Với FHIR WorkflowTask Task mapper, `map-workflow-task-to-fhir.ts` giữ public resource mapper và clinical references; profile, identifier, businessStatus, code, owner và input/output item mapping nằm trong `map-workflow-task-codings.ts`. Chạy `pnpm run harness:domain-composition` khi đổi boundary workflow task FHIR.
- Với FHIR Procedure mapper, `map-procedure-to-fhir.ts` giữ public resource mapper và clinical references; profile, identifier, category label, CodeableConcept conversion và performer mapping nằm trong `map-procedure-codings.ts`. Chạy `pnpm run harness:domain-composition` khi đổi boundary procedure FHIR.
- Với FHIR ProviderDirectory mapper, `map-provider-directory-to-fhir.ts` giữ public resources API và Bundle orchestration; `map-provider-directory-resources.ts` chỉ giữ union/barrel tương thích; Organization, Practitioner, PractitionerRole và Endpoint projection nằm trong các module `map-provider-*-to-fhir.ts` tương ứng; identifier, CodeableConcept, organization type và endpoint connection coding nằm trong `map-provider-directory-codings.ts`. Chạy `pnpm run harness:domain-composition` khi đổi boundary mapping này.
- Với FHIR RecordTransfer Task mapper, `map-record-transfer-to-fhir-task.ts` giữ public mapper orchestration và inter-facility references; Task profile, identifier, status, businessStatus, code và bundle-output coding nằm trong `map-record-transfer-task-codings.ts`; transfer note mapping nằm trong `map-record-transfer-task-notes.ts`. Chạy `pnpm run harness:domain-composition` khi đổi boundary mapping này.
- Với FHIR AuditEvent mapper, `map-audit-event-to-fhir.ts` giữ public mapper và bundle orchestration; profile, event type/subtype, source và bundle identifier nằm trong `map-audit-event-codings.ts`; catalog nhãn hành động audit nằm trong `map-audit-event-labels.ts`; chi tiết entity nằm trong `map-audit-event-details.ts`; action/outcome/purpose coding nằm trong `map-audit-event-outcome.ts`; actor/entity reference nằm trong `map-audit-event-references.ts`. Chạy `pnpm run harness:domain-composition` khi đổi boundary mapping này.
- Chuẩn hóa chuỗi, ngày và số dương dùng chung nằm trong `shared/normalization.ts`; module nghiệp vụ có thể re-export helper cũ để giữ contract nội bộ khi refactor.
- Test tối thiểu cho invariant và mapping liên thông.
